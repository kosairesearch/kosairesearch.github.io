.env
__pycache__/
*.pyc
*.pyo
.DS_Store
data/raw/
venv/
.venv/
data/deploy_functions.log
data/brief_cache/
# 규칙을 통과하지 못한 생성 결과. 사람이 보고 버리는 것이므로 저장소에 남기지 않는다.
data/briefs/_rejected/
# 스테이징 화면 테스트용(jsdom·playwright-core). 저장소에 넣지 않는다 —
# 필요할 때 설치한다. --no-save 로 깔면 package.json 도 생기지 않는다.
node_modules/
package.json
package-lock.json
# 테스트가 잠깐 만드는 작업 폴더.
staging/tests/.work/
staging/tests/.work-co/

# 에뮬레이터가 돌 때 남기는 기록. 검사용이라 저장소에 넣지 않는다.
firestore-debug.log
ui-debug.log
data/_replay/

# GA4 마케팅 자료 — 이 저장소는 공개다. 방문자 수·유입 경로·인기 페이지가
# 그대로 열린다. 2026-09-13 에 76분간 열려 있던 것을 내리고 Firestore 로
# 옮겼다(scripts/ga4_store.py). 여기 파일이 생기는 건 열쇠가 없을 때뿐이다.
data/ga4/
