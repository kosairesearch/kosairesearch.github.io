# 이 저장소에서 일할 때 잊지 말 것

## 유료화(페이월)를 실사이트에 올릴 때 — 사용자에게 꼭 먼저 알릴 것

**`staging/stock.html` 의 리포트 본문 문단 자르는 방식이 실사이트보다 옛날 것이다.**

| 파일 | 문단 자르는 기준 |
|---|---|
| `stock.html` (실사이트) | 170자 예산 — `var PARA_KO=170, PARA_EN=320` 과 `chunkPara(p)` |
| `staging/stock.html` | 3문장마다 — `chunkPara(p, 3)`, `PARA_KO` 없음, `function ps(` 도 없음 |

스테이징 갈래에는 유료화 작업(paywall.js · billing.html · pricing.html ·
demo-backend.js)이 들어 있다. 그래서 언젠가 이 갈래를 실사이트로 올리게 된다.

그때 이 부분을 실사이트 것으로 맞추지 않으면, 사용자가 "리포트 글이 너무
빽빽해서 읽기 힘들다"고 했던 그 상태로 그대로 되돌아간다. 문단이 3문장씩
뭉쳐서 길어지기 때문이다.

**할 일** — 스테이징을 실사이트로 올리기 **전에** `chunkPara`(그리고 같이 쓰는
`PARA_KO`·`PARA_EN`·`ps`)를 실사이트 `stock.html` 버전으로 맞춘다.

*2026-09-12 에 사용자가 "유료화 도입할 때 잊지 말고 꼭 말해달라"고 직접
부탁한 항목이다. 유료화·페이월·구독 관련 작업을 시작하면 먼저 이 항목을
사용자에게 알릴 것.*
