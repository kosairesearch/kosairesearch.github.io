# 이 저장소에서 일할 때 잊지 말 것

## 리포트 본문 문단 자르는 방식 — 실사이트 것이 옳다 *(2026-09-14 해결)*

**끝난 일이다.** 스테이징이 실사이트와 같아졌고, 검사가 지킨다.
아래는 무슨 일이 있었고 무엇이 지키고 있는지에 대한 기록이다.

### 규칙

리포트 본문은 **글자 수(한글 170자 · 영문 320자) 예산**으로 자른다.
문장 중간에서는 절대 자르지 않는다.

    var PARA_KO=170, PARA_EN=320;
    function chunkPara(p){ … }        // prose() 가 쓴다
    function ps(t){ … }               // bull·bear·risk 본문, 종합 의견

세 문장마다 자르던 옛 방식으로 **되돌리지 말 것.** 문장 길이가 제각각이라
한 문단이 휴대폰에서 10~14줄이 되고, 사장이 "리포트 글이 너무 빽빽해서
읽기 힘들다"고 했던 그 상태가 된다.

| | 문단 수 | 중앙값 | 상위 10% | 최장 |
|---|---|---|---|---|
| 세 문장 방식 | 23개 | 282자 (10.3줄) | 389자 (14.4줄) | 658자 |
| 글자 수 방식 | 56개 | 125자 (4.4줄) | 180자 (6.6줄) | 216자 |

*삼성전자 리포트 · 휴대폰 폭 390px 기준*

문단을 나누기만 하고 여백을 안 주면 붙어 버린다. `.prose p` 20px ·
`.factor p` 14px · `.risk .body p` 13px · `.wrapup p` 18px 가 짝이다.

### 무엇이 지키나

`staging/tests/same-paragraphs.test.mjs` — `check_all.sh` 가 같이 돌린다.
양쪽 코드가 같은지 보고, 진짜 리포트 1,800덩어리를 양쪽에 넣어 나온 문단이
글자 하나까지 같은지 본다. 어느 한쪽만 고치면 여기서 걸린다.

### 왜 이런 일이 있었나

실사이트만 글자 수 방식으로 바꾸고 스테이징은 옛 방식으로 남겨 뒀다.
스테이징 갈래에는 유료화 작업(paywall.js · pricing.html · checkout.html ·
demo-backend.js)이 들어 있어서 언젠가 실사이트로 올리게 되는데, 그때 이걸
안 맞추면 빽빽한 상태로 되돌아간다 — 그게 걱정이었다.

막는 것이 이 문서 한 장뿐이었다. 사람이 읽고 기억해야 막히는 구조였다.
2026-09-14 에 사장이 "실사이트에 맞춰야지 당연히" 라고 해서 맞췄고,
기억 대신 검사가 막도록 바꿨다.

*2026-09-12 에 사장이 "유료화 도입할 때 잊지 말고 꼭 말해달라"고 직접
부탁했던 항목이다.*

## 브리핑 생성 규칙·검사기를 고쳤으면 — 그날 안에 live 시험을 돌린다 *(2026-09-14 기록)*

아침 워크플로와 같은 길(회귀 테스트 → 수집 → 생성 → 검사 → 그리기)을
그대로 밟되 발행만 하지 않는 잡이 있다. 규칙을 고친 날 낮에 돌려 보면
다음 날 아침에 글이 검사에 막힐지 미리 안다.

    Actions → 📰 브리핑 다시 써 보기 (수동 · 발행 안 함) → live 켜기
    (.github/workflows/brief_replay.yml · ref 는 작업 가지 · 한 번에 400~900원)

결과 본문은 실행 요약 탭에 찍힌다. 거부됐으면 첨부파일 _rejected/ 에 남는다.

### 왜 있나

9월 14일에 주(週) 검사를 넣고 실사이트에 올린 뒤 이 시험을 돌렸더니
두 번 다 거부돼 글이 안 만들어졌다. "9월 12일(토) 공시가 … 다음 주
월요일까지" 라는 맞는 문장을 검사가 틀렸다고 했다. 그대로 뒀으면 다음 날
아침 브리핑이 빠졌다. 단위 테스트는 전부 통과한 상태였다 — 실제 글은
테스트 문장과 다르게 쓰인다.

### 검사기를 새로 넣을 때 지킬 것

  · 문장을 짐작으로 읽는 검사(주 범위·업종 묶음)는 2차 시도에서 거부
    사유로 치지 않는다(`validate(..., strict_text=False)`). 같은 이유로
    두 번 거부되면 그날 브리핑이 통째로 사라진다. 제목 검사(길이·번역체)는
    2차에도 막는다 — 다만 길이는 2차에 4자 여유를 준다(`HEAD_SLACK`). 9/18 에
    45자 하나로 $0.3 글을 버리고 그날 브리핑을 놓칠 뻔했다.
  · 모르면 판정하지 않는다. 멀리 있는 날짜, 갈라 놓은 종목("옆 업종인")은
    짝짓지 않는다. 거짓 거부 하나가 글 하나를 지운다.

## 브리핑에서 돈이 샜던 자리 *(2026-09-18 기록)*

9/17 콘솔에 $2 가 찍혀 파 보니, 9/18 아침 브리핑 한 편에 글을 **다섯 번
생성해 네 번을 버렸다.** 발행된 한 편 $0.31, 버린 네 편 약 $1.2.

  · 07:15 예비 실행이 07:04 가 올린 글을 못 봤다. `actions/checkout` 의
    기본값이 '실행이 만들어진 순간의 커밋' 이라, concurrency 큐에서 기다린
    실행은 앞 실행의 커밋을 모른다. → `ref: main` 으로 못 박았다. **지우지
    말 것.** 지우면 예비 실행이 매번 같은 글을 또 만든다.
  · 2차 글이 소제목 45자 하나로 거부됐다. → 1차·2차 모두 4자 여유(`HEAD_SLACK`).
    프롬프트는 여전히 44자라고 말한다. 검사만 48자까지 받는다.
  · 버려진 글의 값은 기록에 없었다. → 시도마다 `spent` 에 남기고, 통과한
    글의 meta.usage 에 `attempts`·`usdAll`(·`repaired`) 을 넣는다. 콘솔 청구와
    맞춰 볼 때는 `usdAll` 을 본다.

### 하루에 글 한 편 값만 — 2차는 다시 쓰지 않고 고친다 *(2026-09-18)*

사장: "성공적으로 작성·배포하면서 하루에 1건의 돈만 쓰는 것." 그래서 1차 글이
검사에 걸리면 **Opus 로 다시 쓰지 않는다.** 걸린 자리만 Sonnet 에게 고치게
한다(`repair()` · `REPAIR_MODEL=claude-sonnet-5` · $0.05 안팎). 거부 사유는 대개
'영문이 비었다'·'소제목이 길다'·'업종 묶음' 같은 한두 자리라서 그 자리만 고치면
되고, 잘 쓴 나머지 문단을 새 주사위에 걸지 않는다.

  · 수리 결과는 `{경로: 새 글}` 이고 `apply_patch()` 가 아는 경로만 끼운다.
    모르는 경로·빈 값은 버린다. 그래서 수리 모델이 엉뚱한 키를 내도 글이 안 깨진다.
  · 수리 뒤에도 검사에 걸리면 포기한다(bail). 세 번째 호출은 없다.
  · 수리 호출은 **사고를 끄고**(`thinking: disabled`) **출력 틀**(`output_config.format`)로
    부른다. Sonnet 5 는 thinking 을 비워 두면 사고가 *켜진다* — 9/21 시험에서 출력
    7,745 토큰 중 고친 글은 2,000 남짓, 나머지가 사고·설명이었다($0.13). 둘을 빼지 말 것.
    틀을 API 가 안 받으면(400) 자유 출력으로 한 번 더 부른다 — 그날 글을 잃지 않으려고.
  · Sonnet 5 단가는 $2/$10 이다(9/21 요금표 확인 · `PRICES`). $3/$15 로 되돌리지 말 것.
  · 1차가 파싱·틀(shape) 자체가 깨진 경우만 2차에 다시 쓴다 — 고칠 글이 없어서다.
  · `MAX_TOKENS` 16,000 → 24,000. 사고 토큰도 이 안에서 세므로 재료 많은 날
    잘린 JSON 이 빈 칸 글이 되는 것을 막는다. 쓴 만큼만 내니 돈이 안 든다.

### 글 — 이코노미스트의 목소리 *(2026-09-18, 사장 지시)*

사장: "숫자만 나열하면 재미없다. 왜 올랐고 왜 내렸는지 짧게 붙여라. 이유가 없으면
안 붙여도 된다. 분량은 늘리지 마라. 중국·일본·거시도 한국에 영향 줄 때만 —
매일 넣지 마라. 경제전문가가 돼라."

RULES 의 '숫자와 이유 — 이코노미스트의 규칙' 절이 그 지시다. 이유는 한 구절,
없으면 안 붙임, 숫자는 한 문장에 두어 개(지수 셋 나열 금지), 출처는 문장마다
달지 않음, 아시아·거시는 한국에 닿는 날만. 분량 상한 2,800자(LEN_WANT). 뉴스에
'아시아 증시'·'중국·일본 거시' 갈래를 넣어 재료는 매일 주되 쓸지는 규칙이 정한다.
SYSTEM 프롬프트도 애널리스트 → 이코노미스트로 바꿨다.

9/21 사장: "너무 나열하는 느낌이 강하다." 규칙에 '한 문단에 종목 링크 넷·등락률
대여섯 개까지, 나머지는 업종으로 묶어라' 를 넣고, `check_listing()` 이 링크 6개
이상·등락률 8개 이상인 문단을 잡아 수리(그 문단만 줄임)를 부른다. 2차에는 막지
않는다(문장 검사와 같은 등급). 경계는 프롬프트보다 조금 위다 — 한두 개 차이로
$0.05 를 부르지 않기 위해서.

## 브리핑 재료가 조용히 비는 자리 *(2026-09-22 기록)*

재료는 돈 주고 받는 것이 아니라 공개 사이트에서 긁는다 — 지수는 야후, 수급은
네이버·다음, 뉴스는 구글 뉴스 검색. 상대가 바꾸면 우리 쪽이 빈다. 그리고
"재료 하나 없어도 발행한다" 라서 겉으로는 정상 발행이다. 9/18 에 네이버가 옛
수급 주소를 닫았는데(410) 외국인·기관이 나흘째 빠진 것을 9/22 에야 알았다.

  · **의심되면 먼저 진단을 돌린다.** Actions → 📰 브리핑 다시 써 보기 → `probe`
    (`scripts/probe_sources.py` · 돈 안 듦). 샌드박스는 네이버·다음이 막혀 있어
    러너에서만 답이 나온다. 살았는지 죽었는지, 브리핑이 받을 값까지 찍힌다.
  · 지수 — 야후가 코스피 종가를 하루 늦게 올리는 날이 있다(9/18 · 9/22). 그러면
    네이버 지수 API 에서 그날 값을 받는다(`market_data.naver_index`). 날짜가 맞는
    행이 없으면 지수를 쓰지 않는다 — 다른 날 값을 그날 값인 척 쓰지 않는다.
  · 수급 — `flows.py` 후보 맨 앞 둘이 살아 있는 주소다(네이버 `index/{code}/trend`,
    다음 `market_index/days`). 둘 다 개인·외국인·기관 셋뿐이라 합이 0 이 아니다.
    이름 붙은 키(`NAMED_KEYS`)는 합 검증을 건너뛴다 — 되돌리면 멀쩡한 값이 죽는다.
    거래일이 맞는 행만 쓴다(장중에 부르면 오늘 미완성 행이 맨 위에 온다).
  · 뉴스 — 핵심 갈래(시황·미국 지수)의 좁은 검색어가 0건이면 넓은 예비 검색어로
    한 번 더(`news_data.FALLBACK_KO/EN`). 예비까지 비면 그때 '막혔다' 경보다.

## 유료화를 시작할 때 — 먼저 볼 것 *(2026-09-14 기록, 미뤄 둔 일)*

사장이 "나중에 유료화 생각할 때 하자" 고 미뤄 둔 것들이다. 유료화·페이월·
구독 작업을 시작하면 이 목록부터 사장에게 알릴 것.

### 지금 없는 것 세 가지

9월 6일 개정 때 확인하다 드러났다. 그때는 불리한 변경이 아니라 넘어갔지만
유료 전환은 **불리·중대한 변경**이라 셋 다 있어야 한다.

| | 지금 | 유료화 때 |
|---|---|---|
| 사이트 공지 | 약관 페이지 본문에만 있음 | 배너나 공지사항 페이지가 필요 |
| 개별 통지 | 없음 | 회원 이메일 발송 — 약관 제3조에 스스로 약속함 |
| 재동의 | `CONSENT_VERSION` 이 `2026-08-20` 그대로 | 올려서 다음 로그인 때 재동의 |

`CONSENT_VERSION` 은 `consent.js` · `staging/consent.js` · `functions/index.js`
세 곳에 있다. 셋이 같아야 한다.

### 날짜 규칙

약관 제3조 — 적용일 7일 전(불리·중대한 변경은 **30일 전**)**부터** 공지.
"부터" 라서 **공지를 언제 시작했는지**를 본다.

  · 시행일을 **미루는** 것 — 유예기간이 늘어나므로 처음 공지가 그대로 산다.
    "약관 개정" 이 아니라 "적용일 연기" 로 쓰고, 처음 공지를 지우지 마라.
    지우면 바깥에서는 짧게 공지한 것으로 보인다.
  · 시행일을 **앞당기는** 것 — 유예기간이 줄어드니 새로 공지해야 한다.
  · 날짜를 고치면서 **내용도 손대면** 그 부분은 30일을 다시 센다.

### 개인정보처리방침은 약관과 따로 간다

방침은 **오픈하는 그날** 바꾼다. 30일 전 사전 공지 대상이 아니고, 미리
고치면 그 기간 동안 받지도 않는 정보(결제정보)를 받는다고 적어 둔 상태가
된다. 방침은 실제 처리 현황과 같아야 한다.

### 유료화 개정안은 스테이징에만 얹지 마라

6월에 그렇게 했다가 석 달 동안 갈라진 채로 있었다. 9월 개정이 스테이징에
없어서, 그대로 올렸으면 조용히 지워졌을 것이다.

  초안은 따로 갈래에 두고, 정한 시행일에 실사이트·스테이징을 같이 바꾼다.
  지난 초안: git show 3cf1db027:staging/Terms.html · :staging/Privacy.html
  지키는 검사: staging/tests/same-legal.test.mjs

### 푸터 면책 상자는 뺐다 — 유료화 때 법정 문구로 다시 넣는다 *(2026-09-24)*

사장 결정으로 34장 푸터의 "면책 조항" 상자를 뺐다(`scripts/strip_footer_disclaimer.py` ·
`check_all.sh` 가 되살아나지 않게 본다). 무료인 지금은 요구하는 법이 없다 — 유사투자자문업은
대가가 있어야 신고 대상이고, AI기본법 투명성 의무는 AI 를 도구로 쓰는 쪽에는 없다(과기정통부
가이드라인 2026-01). 남긴 것은 둘뿐이다: 리포트 본문 끝 한 줄(`stock.html #disclaimer` —
회원 아닌 방문자에게 '참고 자료'임을 알리는 유일한 자리)과 약관 제15조.

유료화하면 사정이 바뀐다. 유사투자자문업 신고 뒤에는 "금융투자업자가 아닌 유사투자자문업자 ·
개별 투자 상담 불가 · 원금 손실 가능" 을 광고·화면에 표시해야 한다(2024-08-14 개정 자본시장법).
그때 푸터에 법정 문구를 넣고, About 의 "어떠한 금융업으로도 등록되어 있지 않다" 는 문장도 고친다.

### 개시 전에 끝나 있어야 하는 신고 둘

약관 제9조에 **스스로 약속해 놓았다** — "유료 서비스를 개시하기 전에
통신판매업 신고와 유사투자자문업 신고를 마치고, 그 신고 사항을 서비스
화면에 표시합니다."

푸터의 사업자 정보에 **통신판매업 신고번호**와 **대표전화** 두 줄을 넣어야
한다. 지금은 없어서 뺀 상태다. 넣는 자리는 `scripts/patch_biz_footer.py`
의 `BIZ` 하나뿐이고, 거기만 고쳐 돌리면 34개 푸터가 같이 바뀐다.

*변호사가 아닌 사람이 정리한 것이다. 실제 개정문과 공지 절차는 유료 오픈
전에 변호사가 봐야 한다.*

## 라이트 모드 팔레트 — 바탕 #f9f8f6 · 검정 #141414 *(2026-09-24 사장)*

사장이 레퍼런스 이미지(따뜻한 회백색 바탕 · 먹색 글자)를 주며 라이트 모드의 배경과 검정을 그 색으로
바꾸라고 했다. 이미지에서 잰 값이다 — 바탕 `#f9f8f6` (249,248,246) · 검정 `#141414` (20,20,20).
전에는 `#f2f3f5` · 순검정 `rgb(0,0,0)` 이었다. 되돌리지 말 것.

  · 자리: 각 페이지 `:root` 의 `--fg-1` · `--bg-1`, `body`/`body::after` 배경, 라이트 규칙의 검정 단추
    (`.btn-primary` · `.watch-btn` · 탭 active 등), `auth-guard.js` 의 덮개(페이지색 94%·88%).
  · 반투명 검정 `rgba(0,0,0,.x)` (--fg-2 · --fg-3 · 선 · 그림자) 은 농도라 그대로다. 다크 모드는 손대지 않았다.
  · 랜딩(index.html)은 항상 다크라 대상이 아니다. `r/` 의 SEO 페이지는 따로 된 단순 팔레트라 대상이 아니다.
  · 지키는 검사: `scripts/patch_light_palette.py --check` (`check_all.sh`). 생성기가 옛 CSS 를 베끼거나
    스테이징을 옮기다 옛 색이 돌아오면 여기서 걸린다. 새 페이지는 `python3 scripts/patch_light_palette.py`.

## 휴대폰 사파리의 상태바·주소창 뒤 색 — theme-color 로는 안 된다 *(2026-09-24 기록)*

사장(휴대폰): 라이트↔다크를 누르면 본문은 바뀌는데 위(상태바)·아래(주소창) 뒤는 옛 색이 남았다.
`<meta name="theme-color">` 를 넣고 전환 때 바꿔 봤지만 **iOS 26 사파리(유리 UI)는 그 메타를 쓰지 않는다.**
WebKit 소스(LocalFrameView::fixedContainerEdges)로 확인한 기제:

  · 사파리가 화면 위·아래 가장자리(가운데, 4px 안쪽)를 찍어 거기 닿은 fixed/sticky 요소를 찾고,
    그 요소에 배경색이 있으면 그 색을 쓴다. 없으면(우리 .nav 는 투명) 처음 한 번 픽셀을 찍어 둔 색을
    같은 요소인 동안 계속 쓴다 — 그래서 테마를 바꿔도 옛 색. 새로고침해야 풀린다.
  · 해법: 페이지색 12px·20% 띠 `#kosEdgeTop/#kosEdgeBot` 를 위아래에 둔다(헤더 템플릿 `patch_header.py`).
    배경색이 있는 fixed 요소라 매번 현재 테마 색을 읽는다. 조건은 두께 >10px · 불투명도 ≥.1 · 폭 ≥90%.
    손가락 기기(hover:none·pointer:coarse)에서만, 랜딩은 끈다. theme-color 메타는 다른 브라우저용으로 남긴다.
  · 이 환경에는 사파리가 없다. 실기기(사장 휴대폰)에서만 확인된다. 되돌리거나 띠를 얇게/투명하게 하면 재발한다.

## 스테이징은 새 디자인이다 — 생성기가 만든다, 손으로 고치지 마라 *(2026-09-26 사장 "스테이징에 적용해줘")*

`staging/*.html` 은 이제 손으로 쓰는 파일이 아니다. 시안 생성기(`scripts/build_*_comp.py` + `comp_common.py`)가
`set_mode('staging')` 으로 돌아 만든다 — 상대 경로(`../data` · `Home.html`), 맨 위 STAGING 띠, `demo-backend` · `analytics`
머리 스크립트, 페이지마다 실제 모듈(`auth-state` · `auth-guard` · `watchlist` · `paywall` · `submit-form` · `checkout`),
`?v=` 해시까지 `comp_common.finish()` 와 `stamp_assets` 가 붙인다. 시안(preview/)과 스테이징의 옷은 늘 같다.

    python3 scripts/build_staging.py            # staging/ 을 다시 만든다 (검사 묶음의 "스테이징 생성기"가 같은지 본다)
    python3 scripts/build_404.py                # 루트 404.html (live 모드 — 절대 주소, 띠 없음)

  · 종목 상세는 `scripts/build_stock_staging.py` — 2,682장을 미리 만들지 않고 JS 로 그린다(페이월 때문). 문단 자르기
    함수 셋은 실사이트 stock.html 과 글자 하나까지 같다(`same-paragraphs.test.mjs`).
  · 설정은 페이지(`Settings.html?tab=…`)다. 내용은 `settings-panel.js` 의 `renderSettings` 가 그리고 옷만 새로 입혔다.
    `billing.html` 은 `Settings.html?tab=subscription` 으로 넘기는 껍데기. 헤더 계정 메뉴는 `staging/auth-state.js`(새 DOM용으로
    다시 씀 · 실사이트 auth-state 의 guardConsent 도 들어 있다). 로그인·가입·동의·인증의 실제 Firebase 코드는
    `scripts/auth_staging.py`(문자열 넷) — `build_auth_comp.py` 가 스테이징 모드에서 붙인다.
  · 요금제는 `build_pricing.py`(옛 인라인 모듈 `scripts/pricing_module.js` 그대로), 결제는 `build_checkout.py`(화면은 `checkout.js`).
  · 옛 모듈이 쓰는 토큰 이름(`--fg-1` `--bg-1` `--font-sans` …)은 `comp_common.CSS` 에서 새 토큰에 잇는다. 모듈을 새 이름으로
    고치면 그 다리를 뺀다. 실사이트와 같아야 하는 모듈 다섯(`social-login` `auth-util` `auth-guard` `auth-emails` `consent`)은
    손대지 않았다(`auth-table.test.mjs`).
  · 아침 브리핑 워크플로가 `render_brief.py` 뒤에 `build_staging.py` 를 돌려 `staging/brief.html` 도 같이 올린다.
    옛 `sync_staging_brief.py` 는 지웠다 — 돌리면 옛 디자인이 돌아온다.
  · 검사는 새 구조에 맞췄다: `layout.test.mjs`(스테이징은 `.mmenu` · 눈썹 `.crumb` 일관성), `same-legal.test.mjs`(법률 본문만
    jsdom 으로 비교), `reports-filter.test.mjs`(개수의 쉼표), `same-paragraphs.test.mjs`(`wrapup` 정규식). 요구 사항은 그대로다.
  · 실사이트로 옮기는 날: `comp_common.set_mode('live')` 로 루트에 내면 된다(404.html 이 이미 그 길로 나온다). 그때
    `patch_header.py` · `patch_biz_footer.py` 의 옛 템플릿 검사는 새 구조 검사(생성기 = 저장소)로 바꾼다.
  · 휠 스크롤(2026-09-26 사장): `staging/lenis.js` · `staging/smooth-scroll.js` 는 실사이트 사본이고 `duration` 만 0.7 → 0.6
    (사장 "실사이트보다 조금 더 빠르게" · 휠 한 번 694ms → 614ms). `comp_common.finish()` 가 모든 스테이징 페이지 꼬리에 붙인다.
    휴대폰(pointer:coarse)·움직임 줄임에서는 스스로 꺼진다. 메뉴·업종 시트가 열리면 `KOSSmoothScroll.stop()` — html overflow:hidden
    은 프로그램 스크롤을 못 막아서다. 목차 클릭(TOC_JS)은 lenis 가 있으면 lenis 로 옮기고 전파를 막는다(문서 핸들러의 -90 offset 을
    타면 헤더 여백이 어긋난다). 실사이트로 옮기는 날 0.6 을 실사이트에도 쓸지 정한다.
  · 페이지 전환: `@view-transition{navigation:auto}`(실사이트와 같음) + 띠·헤더에 `view-transition-name` 을 줘 본문만 크로스페이드한다.
  · 로그인 가림창(`.kg-*`)은 `auth-guard.js` 가 그리고 옷만 `comp_common.CSS` 가 덮는다 — 모듈은 실사이트와 같아야 하므로.
    선택자는 `:root[data-theme] body .kg-*` 여야 한다: 모듈의 다크 규칙이 `:root[data-theme=dark] .kg-*`(0,3,0)라 그보다 세야 한다.
    `html[data-theme] body …`(0,2,2)로 적었다가 다크에서 옛 옷이 남았다. 덮개는 헤더 아래부터(z 40)라 메뉴·테마 단추가 산다.

## 종목 페이지는 종목마다 미리 만든다 — r/ 로봇용 사본은 갈 것 *(2026-09-25 사장 "대기업처럼 해줘")*

`stock.html` 은 빈 틀이고 글은 JS 가 그린다. 로봇은 JS 를 안 돌려 `r/{ticker}.html`(로봇용 사본 2,681장)을
따로 두고 있었는데, 사람이 검색으로 그 페이지에 떨어진다. 결정: 큰 회사처럼 종목마다 완성된 HTML 을 미리 만든다.

  · 그리는 모듈 `scripts/stock_page.py` — 시안 한 장(`build_stock_comp.py` → preview/stock.html)과 전 종목 생성기
    (`build_stock_pages.py` → stock/{ticker}.html · assets 한 벌)가 같은 render 를 쓴다. 옷은 한 곳(PAGE_CSS)에만 있다.
  · 등급 셋: 전체(v2) · 옛 형식(v1, 재무 수치 없음) · 리포트 준비 중(새 상장). 셋 다 페이지가 있어야 한다.
  · 지금은 표본 38장만 `preview/stock/` 에 있다(noindex). 전 종목(약 100MB, r/ 와 같음)은 실사이트로 옮기는 날
    `--all --out stock --index` 로. 순서(자동화 교체 · 링크 · 옛 주소 껍데기 · r/ 폐기 · 사이트맵)는
    `docs/design/static-stock-pages.md` 에 있다. 그날까지 `generate_geo_pages.py` 와 r/ 는 그대로 둔다.

## 디자인 헌장 — docs/design/KOSAI-design-charter.md *(2026-09-24, 아직 적용 전)*

사장이 "세계 최고의 웹 디자이너가 되어 모든 이론을 학습하라" 고 해서 12갈래 조사(311 출처) → 종합 → 세 비평 → 수정을
거친 문서다. 한 줄 방향은 "종이에 인쇄된 리서치 리포트". **뼈대(1.1)는 손대지 않고 옷(1.2)만 바꾼다.** 2장의 결정
11개(기본 테마 · 세리프 · 강조색 · 프리즘 · 등락색 · 본문 크기 · 글꼴 CDN · 다크 바탕 · 헤더 활성 · 리포트 구조 · 숫자
글리프)는 **사장이 정하기 전에는 적용하지 않는다.** 정하면 9장의 순서(시안 → 토큰 → 글꼴 → 테마 → …)대로 한 단계에
PR 하나, 스테이징·실사이트 같이. 조사 원자료는 `docs/design/research-findings.json`, 대비 계산기는 `color_calc.py`.

2026-09-25 사장이 디자인을 통째로 맡기며(기능은 유지) "페이지 하나부터 정하자" 고 했다. 그래서 **미리보기 폴더
`preview/`** 를 둔다 — `scripts/make_preview.py <페이지> <덮어쓰기.css>` 가 실사이트 페이지 한 장을 루트 절대
경로로 복사해 스타일을 얹는다(kosai.kr/preview/…, 링크 없음, robots Disallow, noindex). 스테이징은 유료화
작업이 들어 있어 섞지 않는다. 사장이 실기기(사파리)로 보고 정하면 같은 규칙을 실사이트·스테이징에 스크립트로
넣고 이 폴더는 지운다. 검사·패치 스크립트는 `preview/` 를 보지 않는다(루트·staging 만).

### 시각 디자인 총정리 보고서 *(2026-09-26, 사장 "모든 이론과 레퍼런스를 학습해")*

`reports/웹디자인 시각 이론과 레퍼런스 총정리.md` — 16갈래 조사(색·타이포·레이아웃·이미지·3D·모션·질감·데이터시각화·아이콘·토큰·
지각심리·접근성/성능·럭셔리·트렌드·해외/국내 레퍼런스, 사이트 CSS 실측과 대비 계산 포함)를 묶은 것. 노트 원본은 `research_notes/` 같은 이름 폴더.
헌장의 후속편이다: 3-1 절(결정 없이 지금 고칠 것 — 대비 미달 `--ink-55`, `:focus-visible` 0곳, 글꼴 3.1MB preload, 적자 막대 버그 등)은
바로 해도 되고, 3-2 절(헌장 11개 + 새 6개: 브랜드 자산·단추 모양·서명 장치·종이 결·UI 어투·표 단위)은 **사장이 정하기 전 적용하지 않는다.**
